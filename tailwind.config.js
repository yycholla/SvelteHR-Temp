/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{html,js,svelte,ts}'],
  // Tailwind CSS v4 includes all standard utilities by default
  // No need to extend fontSize or spacing - they're built-in
}