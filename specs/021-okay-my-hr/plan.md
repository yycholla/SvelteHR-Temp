# Implementation Plan: Functional System Settings

**Branch**: `021-okay-my-hr` | **Date**: 2025-11-11 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/021-okay-my-hr/spec.md`

## Summary

This feature implements a comprehensive system settings management interface allowing administrators to configure critical system parameters including:

- **System Branding**: Configure system name (default: "MoncuraHR") replacing all hardcoded instances
- **Timezone Management**: Set default timezone with UTC storage and display-time conversion
- **Authentication Policies**: Configure session timeout, password requirements, and account lockout rules
- **Notification Channels**: Configure SMTP email and webhook delivery channels with encrypted credentials
- **Security Settings**: HTTPS enforcement, CORS policies, and security headers
- **Developer Settings**: Log level configuration triggering container restarts in Kubernetes/Docker

**Technical Approach**:

- Backend: Rust GraphQL API with SeaORM migrations, singleton settings table, encrypted credentials
- Frontend: SvelteKit + Svelte 5 with reactive stores, dynamic system name replacement, timezone-aware date formatting
- Deployment: Rolling container restarts via Kubernetes/Docker API for log level changes

## Technical Context

**Language/Version**:

- Backend: Rust 1.75+ with async-graphql, SeaORM, actix-web
- Frontend: TypeScript 5.0, SvelteKit 2.22.0, Svelte 5.0

**Primary Dependencies**:

- Backend: async-graphql 7.0+, sea-orm 0.12+, chrono-tz 0.8+, kube 0.87+ (Kubernetes API), bollard 0.15+ (Docker API), argon2 0.5+ (password hashing)
- Frontend: @urql/svelte 4.0+, zod 4.0+, @internationalized/date 3.5+, svelte-sonner (toast notifications)

**Storage**: PostgreSQL 14+ with pgcrypto extension for credential encryption

**Testing**:

- Backend: cargo test with tokio test runtime
- Frontend: Vitest 3.2+ for unit tests, Playwright 1.49+ for E2E tests
- Contract tests for GraphQL schema validation

**Target Platform**:

- Backend: Linux server (Kubernetes or Docker deployment)
- Frontend: Modern browsers (Chrome 90+, Firefox 88+, Safari 14+)

**Project Type**: Web application (full-stack with separate frontend/backend repositories)

**Performance Goals**:

- Settings queries: <50ms p95 (singleton table lookup)
- Settings updates: <200ms p95 (with validation)
- System name replacement: No perceived latency (reactive store updates)
- Container restarts: <2 minutes for log level propagation (rolling restart)

**Constraints**:

- Session timeout: Enforced within 60 seconds of configured duration
- Password validation: Client-side + server-side validation required
- HTTPS enforcement: Redirect within one round-trip
- Credential encryption: AES-256 via pgcrypto (key in environment variable)
- RBAC: Admin-only mutations enforced via GraphQL guards

**Scale/Scope**:

- Settings: Singleton table (1 row) with <10 QPS (admin-only operations)
- Notification channels: Expected <5 channels total
- Users: Existing users table extended with 4 new columns (indexed)
- Frontend: ~15-20 UI components for settings pages
- Backend: 3 SeaORM migrations, ~10 GraphQL resolvers, 1 middleware for HTTPS enforcement

## Constitution Check

_GATE: Must pass before Phase 0 research. Re-check after Phase 1 design._

**Status**: ✅ Passed (constitution is template placeholder - no violations)

**Notes**:

- No project constitution defined in `.specify/memory/constitution.md` (template only)
- Following existing MountainHR/SvelteHR project patterns
- Maintains consistency with existing RBAC system
- Uses established tech stack (Rust GraphQL + SvelteKit)

## Project Structure

### Documentation (this feature)

```
specs/021-okay-my-hr/
├── spec.md              # Feature specification (completed)
├── plan.md              # This file (implementation plan)
├── research.md          # Phase 0: Technical research (completed)
├── data-model.md        # Phase 1: Database schemas and entities (completed)
├── quickstart.md        # Phase 1: Step-by-step implementation guide (completed)
├── contracts/           # Phase 1: API contracts (completed)
│   └── graphql-api.md   # GraphQL schema, queries, mutations, subscriptions
└── tasks.md             # Phase 2: Task breakdown (to be generated by /tasks)
```

### Source Code (repository root)

This feature spans two repositories:

**Backend Repository** (`../MountainHR-Backend/`):

```
MountainHR-Backend/
├── migration/
│   └── src/
│       ├── m20251111_create_system_settings.rs         # NEW
│       ├── m20251111_create_notification_channels.rs   # NEW
│       └── m20251111_extend_users_for_auth_policies.rs # NEW
├── src/
│   ├── entities/
│   │   ├── system_settings.rs         # NEW (SeaORM entity)
│   │   ├── notification_channels.rs   # NEW (SeaORM entity)
│   │   └── users.rs                   # UPDATED (add auth policy fields)
│   ├── graphql/
│   │   ├── types/
│   │   │   ├── system_settings.rs     # NEW (GraphQL types)
│   │   │   └── notification_channel.rs # NEW (GraphQL types)
│   │   ├── query/
│   │   │   └── system_settings.rs     # NEW (GraphQL queries)
│   │   ├── mutation/
│   │   │   └── system_settings.rs     # NEW (GraphQL mutations)
│   │   └── schema.rs                  # UPDATED (register new resolvers)
│   ├── middleware/
│   │   └── https_enforcer.rs          # NEW (HTTPS redirect middleware)
│   ├── services/
│   │   ├── container_restart.rs       # NEW (K8s/Docker API integration)
│   │   ├── notification_sender.rs     # NEW (SMTP/webhook delivery)
│   │   └── encryption.rs              # NEW (pgcrypto wrapper)
│   └── utils/
│       ├── timezone.rs                # NEW (UTC conversion utilities)
│       └── validators.rs              # UPDATED (add settings validators)
└── tests/
    ├── system_settings_test.rs        # NEW (unit tests)
    └── integration_test.rs            # UPDATED (add settings API tests)
```

**Frontend Repository** (`./`):

```
SvelteHR/
├── src/
│   ├── lib/
│   │   ├── graphql/
│   │   │   ├── operations.ts          # UPDATED (add settings queries/mutations)
│   │   │   └── client.ts              # EXISTING (urql client setup)
│   │   ├── stores/
│   │   │   └── system-settings.ts     # NEW (reactive settings store)
│   │   ├── schemas/
│   │   │   └── system-settings.ts     # NEW (Zod validation schemas)
│   │   ├── utils/
│   │   │   ├── session-manager.ts     # NEW (session timeout enforcement)
│   │   │   ├── password-validator.ts  # NEW (dynamic password validation)
│   │   │   └── timezone.ts            # NEW (timezone-aware date formatting)
│   │   └── components/
│   │       ├── ui/                    # EXISTING (base UI components)
│   │       └── settings/              # NEW (settings-specific components)
│   │           ├── GeneralSettings.svelte
│   │           ├── AuthSettings.svelte
│   │           ├── NotificationSettings.svelte
│   │           ├── SecuritySettings.svelte
│   │           ├── DeveloperSettings.svelte
│   │           ├── EmailChannelForm.svelte
│   │           └── WebhookChannelForm.svelte
│   └── routes/
│       ├── admin/
│       │   └── settings/
│       │       ├── +page.svelte        # NEW (main settings page)
│       │       ├── +page.server.ts     # NEW (server-side data loading)
│       │       └── +layout.svelte      # NEW (settings layout with tabs)
│       ├── login/
│       │   └── +page.svelte            # UPDATED (add account lockout UI)
│       └── +layout.svelte              # UPDATED (replace hardcoded system name)
└── tests/
    ├── unit/
    │   └── settings-store.spec.ts      # NEW (store tests)
    └── e2e/
        └── system-settings.spec.ts     # NEW (E2E tests)
```

**Structure Decision**:

- **Web application pattern** (separate backend and frontend repositories)
- Backend: Rust GraphQL API in adjacent `MountainHR-Backend` directory
- Frontend: SvelteKit app in current directory
- Follows existing project structure and conventions
- 3 new backend migrations, ~10 new GraphQL resolvers, ~7 new frontend components
- Minimal modifications to existing files (users table extension, layout updates)

## Complexity Tracking

_No violations - constitution is template only, no rules defined._

**Architectural Decisions**:

| Decision                         | Rationale                                                         | Trade-offs                                                       |
| -------------------------------- | ----------------------------------------------------------------- | ---------------------------------------------------------------- |
| Singleton SystemSettings table   | Single source of truth, simple queries                            | Cannot support multi-tenant settings in future without migration |
| UTC timestamp storage            | Timezone-independent data, avoid DST issues                       | Conversion overhead on every display (acceptable for low QPS)    |
| GraphQL API (not REST)           | Matches existing backend architecture                             | Learning curve for developers unfamiliar with GraphQL            |
| Best-effort webhook delivery     | Simplifies implementation, aligns with notification priority (P2) | No retry logic means notifications may be lost                   |
| Container restart for log levels | Reliable environment variable propagation                         | Brief service interruption (~2 min rolling restart)              |
| pgcrypto encryption              | Database-level security, no application key management            | Performance overhead for config reads (acceptable for low QPS)   |

## Progress Tracking

| Phase                       | Status      | Artifacts Generated                                    | Date Completed |
| --------------------------- | ----------- | ------------------------------------------------------ | -------------- |
| **Phase 0: Research**       | ✅ Complete | research.md                                            | 2025-11-11     |
| **Phase 1: Design**         | ✅ Complete | data-model.md, contracts/graphql-api.md, quickstart.md | 2025-11-11     |
| **Phase 2: Tasks**          | ⏸️ Pending  | tasks.md (via /tasks command)                          | -              |
| **Phase 3: Implementation** | ⏸️ Pending  | Source code                                            | -              |
| **Phase 4: Testing**        | ⏸️ Pending  | Test results, E2E coverage                             | -              |
| **Phase 5: Deployment**     | ⏸️ Pending  | Production deployment                                  | -              |

## Implementation Phases

### Phase 1: Database & Backend Foundation (Priority: P1)

**Estimated Duration**: 1.5 days

**Tasks**:

1. Create SeaORM migrations (system_settings, notification_channels, users extensions)
2. Run migrations and verify database schema
3. Generate SeaORM entities
4. Implement GraphQL types for SystemSettings and NotificationChannel
5. Implement systemSettings query with Admin RBAC guard
6. Implement updateSystemSettings mutation with validation
7. Write unit tests for validation logic
8. Test GraphQL API via Playground

**Deliverables**:

- 3 migration files
- 3 entity files (1 new, 2 updated)
- GraphQL schema with queries/mutations
- Unit tests passing

**Dependencies**: PostgreSQL database, Rust toolchain

---

### Phase 2: Frontend Settings UI (Priority: P1)

**Estimated Duration**: 1 day

**Tasks**:

1. Create system-settings store with urql query
2. Create SystemSettings GraphQL operations (queries/mutations)
3. Create Zod validation schemas
4. Build settings page layout with tabs
5. Implement GeneralSettings component (system name, timezone)
6. Implement AuthSettings component (session timeout, password length, max attempts)
7. Implement DeveloperSettings component (log levels)
8. Wire up form submission to updateSystemSettings mutation
9. Add toast notifications for success/error states
10. Write component unit tests

**Deliverables**:

- Settings store
- 7 Svelte components
- GraphQL operations
- Unit tests for store and validation

**Dependencies**: Phase 1 complete (backend API available)

---

### Phase 3: System Name Replacement (Priority: P1)

**Estimated Duration**: 0.5 days

**Tasks**:

1. Scan codebase for hardcoded "MountainHR" and "SvelteHR" instances
2. Replace with reactive `$systemName` store subscriptions
3. Update app.html title with dynamic meta tag
4. Update email templates with system name variable
5. Test branding changes across all pages
6. Write E2E test for system name update

**Deliverables**:

- All hardcoded instances replaced
- E2E test for branding

**Dependencies**: Phase 2 complete (settings store available)

---

### Phase 4: Timezone & UTC Storage (Priority: P1)

**Estimated Duration**: 0.5 days

**Tasks**:

1. Implement timezone conversion utilities (UTC → display time)
2. Update date formatting functions to use systemTimezone
3. Verify all existing timestamps use UTC storage (database audit)
4. Update GraphQL resolvers to return ISO 8601 UTC strings
5. Test timezone changes with existing timestamped data
6. Write unit tests for timezone conversion

**Deliverables**:

- Timezone utility functions
- Updated date formatters
- Unit tests

**Dependencies**: Phase 1 complete

---

### Phase 5: Authentication Policy Enforcement (Priority: P1)

**Estimated Duration**: 1 day

**Tasks**:

1. Implement session timeout manager (activity tracking + auto-logout)
2. Integrate session timeout with JWT expiration
3. Implement password validation utility (dynamic min length)
4. Update password change forms with new validation
5. Implement account lockout logic in login mutation
6. Add failed_login_attempts increment on auth failure
7. Implement unlockUserAccount mutation
8. Update login UI with lockout error message
9. Write unit tests for lockout logic
10. Write E2E test for session timeout and account lockout

**Deliverables**:

- Session manager
- Password validator
- Account lockout backend logic
- unlockUserAccount mutation
- E2E tests

**Dependencies**: Phase 1 complete (users table extended)

---

### Phase 6: Notification Channels (Priority: P2)

**Estimated Duration**: 1.5 days

**Tasks**:

1. Implement createNotificationChannel mutation
2. Implement SMTP credential validation (test connection)
3. Implement credential encryption/decryption (pgcrypto wrapper)
4. Implement sendTestEmail mutation
5. Implement best-effort webhook delivery service
6. Build NotificationSettings component
7. Build EmailChannelForm and WebhookChannelForm
8. Integrate notification sending in existing event handlers
9. Write unit tests for SMTP validation
10. Write E2E test for email channel configuration

**Deliverables**:

- Notification channel mutations
- SMTP/webhook services
- Encryption utilities
- Frontend forms
- Unit and E2E tests

**Dependencies**: Phase 1 complete

---

### Phase 7: Security Settings (Priority: P2)

**Estimated Duration**: 0.5 days

**Tasks**:

1. Implement HTTPS enforcement middleware
2. Implement CORS configuration middleware
3. Implement security headers middleware (CSP, X-Frame-Options, HSTS)
4. Build SecuritySettings component
5. Test HTTPS redirect logic
6. Write unit tests for middleware

**Deliverables**:

- 3 middleware modules
- SecuritySettings component
- Unit tests

**Dependencies**: Phase 1 complete

---

### Phase 8: Log Level Container Restart (Priority: P3)

**Estimated Duration**: 1 day

**Tasks**:

1. Implement Kubernetes API client integration (kube crate)
2. Implement Docker API client integration (bollard crate)
3. Implement container restart service (detect K8s vs Docker)
4. Update updateSystemSettings mutation to trigger restart on log level change
5. Test rolling restart in development environment
6. Add logging for restart operations
7. Write integration test for restart trigger

**Deliverables**:

- Container restart service
- K8s/Docker API integration
- Integration tests

**Dependencies**: Phase 2 complete, Kubernetes/Docker environment available

---

### Phase 9: E2E Testing & QA (Priority: P1)

**Estimated Duration**: 1 day

**Tasks**:

1. Write comprehensive E2E test suite (all user stories)
2. Test system name replacement across all pages
3. Test timezone changes with existing data
4. Test session timeout enforcement
5. Test account lockout and unlock
6. Test SMTP configuration and test email
7. Test webhook delivery
8. Test HTTPS enforcement
9. Test log level changes and container restart
10. Fix any bugs discovered during testing

**Deliverables**:

- Complete E2E test suite
- Bug fixes
- Test coverage report

**Dependencies**: All previous phases complete

---

### Phase 10: Documentation & Deployment (Priority: P1)

**Estimated Duration**: 0.5 days

**Tasks**:

1. Update README with new settings feature
2. Document environment variables (SETTINGS_ENCRYPTION_KEY, etc.)
3. Create deployment runbook for production
4. Run database migrations in production
5. Deploy backend API
6. Deploy frontend
7. Verify production deployment
8. Monitor for errors in first 24 hours

**Deliverables**:

- Updated documentation
- Deployment runbook
- Production deployment

**Dependencies**: Phase 9 complete (all tests passing)

## Risk Mitigation

| Risk                                          | Probability | Impact   | Mitigation Strategy                                     |
| --------------------------------------------- | ----------- | -------- | ------------------------------------------------------- |
| Container restart causes downtime             | Medium      | High     | Use Kubernetes rolling restart, warn users in UI        |
| Encryption key compromise                     | Low         | Critical | Store key in K8s secrets, implement key rotation script |
| Invalid timezone breaks app                   | Low         | High     | Validate against IANA database, fallback to UTC         |
| SMTP credentials leak in logs                 | Medium      | High     | Never log config_json, encrypt at rest, mask in errors  |
| Singleton constraint violated                 | Low         | Medium   | Database CHECK constraint enforces, test in CI          |
| System name XSS attack                        | Low         | High     | Sanitize input, escape in Svelte templates (automatic)  |
| Session timeout race condition                | Medium      | Low      | Use atomic token refresh, graceful logout modal         |
| Password grandfathering allows weak passwords | Low         | Medium   | Document policy, allow admin to force password reset    |

## Testing Strategy

### Unit Tests

**Backend**:

- SeaORM entity CRUD operations
- GraphQL resolver validation logic
- Timezone conversion utilities
- Password validation logic
- SMTP validation logic
- Encryption/decryption utilities

**Frontend**:

- Settings store behavior
- Form validation with Zod schemas
- Date formatting utilities
- Component rendering with mock data

### Integration Tests

**Backend**:

- GraphQL API end-to-end (query → mutation → database)
- Container restart integration (K8s/Docker API calls)
- SMTP connection testing
- Webhook delivery testing

**Frontend**:

- Server-side data loading
- GraphQL client integration
- Form submission flow

### E2E Tests (Playwright)

1. Admin updates system name → Name appears in header/footer
2. Admin changes timezone → Timestamps display in new timezone
3. Admin sets session timeout → User auto-logged out after timeout
4. Admin sets password length → Validation enforced on password change
5. User fails login 5 times → Account locked → Admin unlocks
6. Admin configures SMTP → Test email delivered successfully
7. Admin configures webhook → Notification sent to endpoint
8. Admin changes log level → Containers restart with new level
9. Admin enables HTTPS → HTTP requests redirected to HTTPS

### Contract Tests

- GraphQL schema matches spec
- RBAC authorization enforced on mutations
- Input validation returns correct error codes

## Success Criteria

| Criteria                     | Measurement                          | Target              |
| ---------------------------- | ------------------------------------ | ------------------- |
| System name update latency   | Time from save to UI update          | <5 seconds          |
| Timezone conversion accuracy | Data integrity after timezone change | 100% (no data loss) |
| Session timeout precision    | Logout within configured duration    | ±60 seconds         |
| Password validation accuracy | Rejection of short passwords         | 100%                |
| Account lockout enforcement  | Lock after max attempts exceeded     | Immediate           |
| Email delivery rate          | SMTP send success                    | >95%                |
| Webhook delivery rate        | HTTP POST success                    | >95%                |
| Log level propagation time   | Container restart completion         | <2 minutes          |
| HTTPS redirect speed         | HTTP→HTTPS round trip                | <1 round trip       |
| Admin configuration success  | First-attempt completion             | >90%                |

## Post-Launch Monitoring

**Key Metrics**:

- Settings update frequency (QPS)
- Session timeout events per day
- Account lockout events per day
- Notification delivery success rate
- Container restart duration
- Settings query latency (p50, p95, p99)

**Alerts**:

- Settings query latency > 100ms
- SMTP authentication failure rate > 10%
- Webhook delivery failure rate > 20%
- Container restart duration > 5 minutes

## Future Enhancements

**Not in MVP** (deferred to post-launch):

1. **Audit Log Table**: Track all settings changes with user, timestamp, old/new values
2. **Per-User Timezone Preferences**: Allow users to override system timezone
3. **Password Expiration Enforcement**: Force password change after N days
4. **MFA Enforcement**: Require MFA for all users (currently just a checkbox)
5. **Multi-Tenant Settings**: Support per-organization settings (requires major refactor)
6. **GraphQL Subscriptions**: Real-time settings updates via subscriptions
7. **Settings Export/Import**: Backup and restore settings configuration
8. **CORS UI Editor**: Visual editor for CORS origins (currently text array)

## References

- Feature Specification: [spec.md](./spec.md)
- Technical Research: [research.md](./research.md)
- Data Model: [data-model.md](./data-model.md)
- API Contract: [contracts/graphql-api.md](./contracts/graphql-api.md)
- Quick Start Guide: [quickstart.md](./quickstart.md)

**External Documentation**:

- SeaORM Migration Guide: https://www.sea-ql.org/SeaORM/docs/migration/writing-migration/
- async-graphql Authorization: https://async-graphql.github.io/async-graphql/en/context_and_data_loaders.html
- Kubernetes Client Rust: https://github.com/kube-rs/kube
- IANA Timezone Database: https://www.iana.org/time-zones
- PostgreSQL pgcrypto: https://www.postgresql.org/docs/current/pgcrypto.html

---

**Total Estimated Duration**: 9-11 days
**Critical Path**: Phase 1 → Phase 2 → Phase 3 → Phase 5 → Phase 9 → Phase 10
**Parallel Work Opportunities**: Phases 4, 6, 7, 8 can be developed in parallel after Phase 1
