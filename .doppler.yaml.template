# =============================================================================
# Doppler Configuration Template for SvelteHR Frontend
# =============================================================================
# 
# This template shows the Doppler configuration structure.
# Copy this to .doppler.yaml and configure your project settings.
#
# Setup Instructions:
#   1. Copy this file: cp .doppler.yaml.template .doppler.yaml
#   2. Update the project name if needed
#   3. Run: make doppler-setup
#   4. Configure secrets in Doppler dashboard or CLI
# =============================================================================

# Doppler project configuration
setup:
  project: mountainhr-frontend
  config:
    development:
      name: dev
    staging:
      name: stg  
    production:
      name: prd

# Default configuration for local development
project: mountainhr-frontend
config: dev