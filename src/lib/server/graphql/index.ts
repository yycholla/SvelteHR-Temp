/**
 * Server-side GraphQL utilities
 *
 * Provides standardized GraphQL client and query building functionality
 * for server-side route handlers and API endpoints.
 */

export { UnifiedGraphQLClient, createGraphQLClient } from './unified-client';
export { GraphQLQueryBuilder } from './query-builder';
