# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Rust files (Prettier doesn't format Rust)
**/*.rs
**/target/
Cargo.lock

# SQL files (backend database files)
**/*.sql
seeds/**
graphql-rust-server/scripts/**/*.sql

# OpenCode/Specify templates (MD files with custom syntax)
.opencode/**
.specify/**

# Storybook (MDX files may have SQL/custom syntax)
.storybook/**

# MCP config (JSON with comments)
.mcp.json

# GitHub workflows (YAML formatting managed separately)
.github/**

# Kubernetes manifests (YAML with Helm/Kustomize templates, use helm lint/kustomize validate instead)
k8s/**
