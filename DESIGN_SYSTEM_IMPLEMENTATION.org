#+TITLE: SvelteHR Design System Implementation Plan
#+AUTHOR: Claude Code
#+DATE: 2025-08-05
#+STARTUP: overview
#+TODO: TODO NEXT DOING | DONE CANCELLED

* Overview
This document outlines the comprehensive implementation plan for establishing a robust design system foundation for the SvelteHR component library and enhancing existing components to production standards.

** Current State
- SvelteHR: Svelte-based HR application
- Component library location: ~/src/lib/components/~
- 18 existing UI components built on bits-ui and Tailwind CSS
- Components: alert, avatar, badge, button, card, checkbox, dialog, input, label, progress, radio-group, select, separator, skeleton, switch, tabs, textarea, tooltip
- Additional: Calendar and login components

* Phase 1: Design System Foundation [0/4]

** TODO Design Token System Implementation
*** Core Token Categories
**** Color System
***** Primary Colors (Trust & Stability)
- Primary: Blue-based palette (#3B82F6, #1E40AF, #1D4ED8)
- Secondary: Complementary grays (#6B7280, #374151, #111827)

***** Semantic Colors
- Success: #10B981 (achievements, approvals)
- Warning: #F59E0B (attention, pending actions)
- Danger: #EF4444 (critical actions, errors)
- Info: #3B82F6 (information, notifications)

***** HR-Specific Color Coding
- Departments: Engineering (#10B981), HR (#8B5CF6), Finance (#F59E0B), Sales (#EF4444)
- Employee Status: Active (#10B981), Inactive (#6B7280), Pending (#F59E0B)
- Leave Types: Vacation (#3B82F6), Sick (#EF4444), Personal (#8B5CF6)

**** Typography Scale
***** Font Families
- Headers: Inter, system-ui, sans-serif
- Body: Inter, system-ui, sans-serif  
- Monospace: 'JetBrains Mono', Consolas, monospace

***** Size Scale (rem units)
- xs: 0.75rem (12px)
- sm: 0.875rem (14px)
- base: 1rem (16px)
- lg: 1.125rem (18px)
- xl: 1.25rem (20px)
- 2xl: 1.5rem (24px)
- 3xl: 1.875rem (30px)
- 4xl: 2.25rem (36px)

***** Font Weights
- light: 300
- normal: 400
- medium: 500
- semibold: 600
- bold: 700

**** Spacing System
***** Base Scale (4px increments)
- xs: 0.25rem (4px)
- sm: 0.5rem (8px)
- md: 1rem (16px)
- lg: 1.5rem (24px)
- xl: 2rem (32px)
- 2xl: 3rem (48px)
- 3xl: 4rem (64px)

**** Visual Effects
***** Border Radius
- sm: 4px
- md: 8px
- lg: 12px
- xl: 16px
- full: 9999px

***** Shadows
- sm: 0 1px 2px 0 rgb(0 0 0 / 0.05)
- md: 0 4px 6px -1px rgb(0 0 0 / 0.1)
- lg: 0 10px 15px -3px rgb(0 0 0 / 0.1)

***** Motion
- fast: 150ms
- base: 250ms
- slow: 350ms
- ease: cubic-bezier(0.4, 0, 0.2, 1)

*** Implementation Files
**** ~/src/lib/styles/design-tokens.css~
#+BEGIN_SRC css
:root {
  /* Colors - Primary */
  --color-primary-50: #EFF6FF;
  --color-primary-100: #DBEAFE;
  --color-primary-500: #3B82F6;
  --color-primary-600: #2563EB;
  --color-primary-900: #1E3A8A;
  
  /* Colors - Semantic */
  --color-success: #10B981;
  --color-warning: #F59E0B;
  --color-danger: #EF4444;
  --color-info: #3B82F6;
  
  /* Typography */
  --font-family-sans: Inter, system-ui, sans-serif;
  --font-family-mono: 'JetBrains Mono', Consolas, monospace;
  
  --font-size-xs: 0.75rem;
  --font-size-sm: 0.875rem;
  --font-size-base: 1rem;
  --font-size-lg: 1.125rem;
  --font-size-xl: 1.25rem;
  
  /* Spacing */
  --spacing-xs: 0.25rem;
  --spacing-sm: 0.5rem;
  --spacing-md: 1rem;
  --spacing-lg: 1.5rem;
  --spacing-xl: 2rem;
  
  /* Effects */
  --border-radius-sm: 4px;
  --border-radius-md: 8px;
  --border-radius-lg: 12px;
  
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  
  --transition-fast: 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-base: 250ms cubic-bezier(0.4, 0, 0.2, 1);
}

[data-theme="dark"] {
  --color-primary-50: #1E3A8A;
  --color-primary-100: #1D4ED8;
  /* Dark mode overrides */
}
#+END_SRC

**** ~/src/lib/types/design-system.ts~
#+BEGIN_SRC typescript
export type ColorScale = 
  | '50' | '100' | '200' | '300' | '400' 
  | '500' | '600' | '700' | '800' | '900'

export type SemanticColor = 'success' | 'warning' | 'danger' | 'info'

export type FontSize = 'xs' | 'sm' | 'base' | 'lg' | 'xl' | '2xl' | '3xl' | '4xl'

export type Spacing = 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl' | '3xl'

export type BorderRadius = 'sm' | 'md' | 'lg' | 'xl' | 'full'

export interface DesignTokens {
  colors: {
    primary: Record<ColorScale, string>
    semantic: Record<SemanticColor, string>
    departments: Record<string, string>
  }
  typography: {
    fontFamily: Record<string, string>
    fontSize: Record<FontSize, string>
    fontWeight: Record<string, number>
  }
  spacing: Record<Spacing, string>
  effects: {
    borderRadius: Record<BorderRadius, string>
    shadow: Record<string, string>
    transition: Record<string, string>
  }
}
#+END_SRC

** TODO Component Architecture Standards
*** Variant System Implementation
**** Size Variants
- xs: Extra small (compact data views)
- sm: Small (dense layouts)
- md: Medium (default)
- lg: Large (prominent actions)
- xl: Extra large (hero elements)

**** State Management
- default: Normal interactive state
- loading: Processing with spinner/skeleton
- disabled: Non-interactive with visual indication
- error: Invalid state with error styling
- success: Successful action with confirmation

*** Accessibility Standards
**** ARIA Implementation
- Proper labeling with ~aria-label~ and ~aria-labelledby~
- State communication with ~aria-expanded~, ~aria-selected~
- Role definitions for custom components
- Focus management for modal and dropdown components

**** Keyboard Navigation
- Tab order optimization
- Enter/Space activation for buttons
- Arrow key navigation for lists and menus
- Escape key handling for overlays

*** Theming System
**** Theme Toggle Implementation
#+BEGIN_SRC typescript
// Theme store
import { writable } from 'svelte/store'

export type Theme = 'light' | 'dark' | 'auto'

export const theme = writable<Theme>('auto')

export function toggleTheme() {
  theme.update(current => current === 'light' ? 'dark' : 'light')
}

export function applyTheme(selectedTheme: Theme) {
  const root = document.documentElement
  
  if (selectedTheme === 'auto') {
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches
    root.setAttribute('data-theme', prefersDark ? 'dark' : 'light')
  } else {
    root.setAttribute('data-theme', selectedTheme)
  }
}
#+END_SRC

** TODO Enhanced Component Implementations
*** Priority Components for Enhancement

**** Button Component Enhancement
***** New Variants
- ~loading~: Shows spinner, disables interaction
- ~icon-only~: Square aspect ratio for icons
- ~split~: Dropdown indicator on right side

***** Button Group Support
#+BEGIN_SRC svelte
<!-- ButtonGroup.svelte -->
<script lang="ts">
  export let size: 'sm' | 'md' | 'lg' = 'md'
  export let variant: 'outline' | 'solid' = 'outline'
</script>

<div class="button-group" class:size-{size} class:variant-{variant}>
  <slot />
</div>

<style>
  .button-group {
    display: inline-flex;
    border-radius: var(--border-radius-md);
    overflow: hidden;
  }
  
  .button-group :global(button:not(:last-child)) {
    border-right: 0;
    border-radius: 0;
  }
  
  .button-group :global(button:first-child) {
    border-top-left-radius: var(--border-radius-md);
    border-bottom-left-radius: var(--border-radius-md);
  }
  
  .button-group :global(button:last-child) {
    border-top-right-radius: var(--border-radius-md);
    border-bottom-right-radius: var(--border-radius-md);
  }
</style>
#+END_SRC

**** Input Component Enhancement
***** Input Group Pattern
#+BEGIN_SRC svelte
<!-- InputGroup.svelte -->
<script lang="ts">
  export let prefix: string = ''
  export let suffix: string = ''
  export let error: string = ''
</script>

<div class="input-group" class:has-error={error}>
  {#if prefix}
    <span class="input-prefix">{prefix}</span>
  {/if}
  
  <slot />
  
  {#if suffix}
    <span class="input-suffix">{suffix}</span>
  {/if}
</div>

{#if error}
  <p class="input-error" role="alert">{error}</p>
{/if}

<style>
  .input-group {
    display: flex;
    align-items: center;
    border: 1px solid var(--color-gray-300);
    border-radius: var(--border-radius-md);
    background: white;
    transition: var(--transition-fast);
  }
  
  .input-group:focus-within {
    border-color: var(--color-primary-500);
    box-shadow: 0 0 0 3px var(--color-primary-100);
  }
  
  .input-group.has-error {
    border-color: var(--color-danger);
  }
  
  .input-prefix, .input-suffix {
    padding: 0 var(--spacing-sm);
    color: var(--color-gray-500);
    font-size: var(--font-size-sm);
  }
  
  .input-error {
    margin-top: var(--spacing-xs);
    color: var(--color-danger);
    font-size: var(--font-size-sm);
  }
</style>
#+END_SRC

**** Card Component Enhancement
***** Interactive Card Variant
#+BEGIN_SRC svelte
<!-- Card.svelte - Enhanced -->
<script lang="ts">
  export let variant: 'default' | 'interactive' | 'loading' = 'default'
  export let padding: 'sm' | 'md' | 'lg' = 'md'
  export let href: string | undefined = undefined
  export let onClick: (() => void) | undefined = undefined
  
  $: interactive = variant === 'interactive' || href || onClick
  $: Component = href ? 'a' : 'div'
</script>

<svelte:element 
  this={Component}
  {href}
  class="card"
  class:interactive
  class:loading={variant === 'loading'}
  class:padding-{padding}
  on:click={onClick}
  tabindex={interactive ? 0 : -1}
  role={interactive ? 'button' : undefined}
>
  {#if variant === 'loading'}
    <div class="loading-skeleton">
      <div class="skeleton-line"></div>
      <div class="skeleton-line short"></div>
    </div>
  {:else}
    <slot />
  {/if}
</svelte:element>

<style>
  .card {
    background: white;
    border: 1px solid var(--color-gray-200);
    border-radius: var(--border-radius-lg);
    box-shadow: var(--shadow-sm);
    transition: var(--transition-base);
  }
  
  .card.interactive:hover {
    box-shadow: var(--shadow-md);
    transform: translateY(-1px);
  }
  
  .card.interactive:focus {
    outline: 2px solid var(--color-primary-500);
    outline-offset: 2px;
  }
  
  .padding-sm { padding: var(--spacing-sm); }
  .padding-md { padding: var(--spacing-md); }
  .padding-lg { padding: var(--spacing-lg); }
  
  .loading-skeleton {
    animation: pulse 2s infinite;
  }
  
  .skeleton-line {
    height: 1rem;
    background: var(--color-gray-200);
    border-radius: var(--border-radius-sm);
    margin-bottom: var(--spacing-sm);
  }
  
  .skeleton-line.short {
    width: 60%;
  }
  
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }
</style>
#+END_SRC

** TODO CSS Architecture Implementation
*** File Structure
**** ~/src/lib/styles/index.css~ (Main entry point)
#+BEGIN_SRC css
/* Design System Styles */
@import './design-tokens.css';
@import './base.css';
@import './components.css';
@import './utilities.css';

/* Tailwind CSS */
@tailwind base;
@tailwind components;
@tailwind utilities;
#+END_SRC

**** ~/src/lib/styles/base.css~ (Base styles)
#+BEGIN_SRC css
/* Base styles using design tokens */
html {
  font-family: var(--font-family-sans);
  line-height: 1.6;
}

body {
  color: var(--color-gray-900);
  background-color: var(--color-gray-50);
  font-size: var(--font-size-base);
}

/* Focus styles */
*:focus {
  outline: 2px solid var(--color-primary-500);
  outline-offset: 2px;
}

/* Reduced motion support */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
#+END_SRC

**** ~/src/lib/styles/utilities.css~ (Utility classes)
#+BEGIN_SRC css
/* Spacing utilities */
.space-xs { gap: var(--spacing-xs); }
.space-sm { gap: var(--spacing-sm); }
.space-md { gap: var(--spacing-md); }
.space-lg { gap: var(--spacing-lg); }

/* Typography utilities */
.text-xs { font-size: var(--font-size-xs); }
.text-sm { font-size: var(--font-size-sm); }
.text-base { font-size: var(--font-size-base); }
.text-lg { font-size: var(--font-size-lg); }

/* Color utilities */
.text-primary { color: var(--color-primary-600); }
.text-success { color: var(--color-success); }
.text-warning { color: var(--color-warning); }
.text-danger { color: var(--color-danger); }

/* Layout utilities */
.stack {
  display: flex;
  flex-direction: column;
}

.cluster {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
}

/* Accessibility utilities */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
#+END_SRC

* Phase 2: Component Standards Implementation [0/3]

** TODO Implement Consistent Component API
*** Standard Props Interface
#+BEGIN_SRC typescript
// Base component interface
export interface BaseComponentProps {
  size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl'
  variant?: string
  disabled?: boolean
  loading?: boolean
  class?: string
  id?: string
  'data-testid'?: string
}

// Form component interface
export interface FormComponentProps extends BaseComponentProps {
  name?: string
  value?: any
  placeholder?: string
  required?: boolean
  readonly?: boolean
  error?: string
  'aria-label'?: string
  'aria-describedby'?: string
}
#+END_SRC

** TODO Create Component Composition Patterns
*** Compound Component Pattern
#+BEGIN_SRC svelte
<!-- Example: Alert compound component -->
<!-- Alert.svelte -->
<script context="module" lang="ts">
  export { default as AlertTitle } from './AlertTitle.svelte'
  export { default as AlertDescription } from './AlertDescription.svelte'
</script>

<script lang="ts">
  export let variant: 'info' | 'success' | 'warning' | 'danger' = 'info'
  export let dismissible = false
  
  let dismissed = false
  
  function dismiss() {
    dismissed = true
  }
</script>

{#if !dismissed}
  <div 
    class="alert"
    class:variant-{variant}
    role="alert"
    aria-live="polite"
  >
    <slot />
    
    {#if dismissible}
      <button 
        class="alert-dismiss"
        on:click={dismiss}
        aria-label="Dismiss alert"
      >
        ×
      </button>
    {/if}
  </div>
{/if}
#+END_SRC

** TODO Testing Infrastructure Setup
*** Component Testing Template
#+BEGIN_SRC typescript
// Example test file: Button.test.ts
import { render, screen, fireEvent } from '@testing-library/svelte'
import { describe, it, expect, vi } from 'vitest'
import Button from './Button.svelte'

describe('Button Component', () => {
  it('renders with correct text', () => {
    render(Button, { children: 'Click me' })
    expect(screen.getByRole('button')).toHaveTextContent('Click me')
  })
  
  it('handles click events', async () => {
    const handleClick = vi.fn()
    render(Button, { 
      onclick: handleClick,
      children: 'Click me'
    })
    
    await fireEvent.click(screen.getByRole('button'))
    expect(handleClick).toHaveBeenCalledOnce()
  })
  
  it('shows loading state', () => {
    render(Button, { 
      loading: true,
      children: 'Loading...'
    })
    
    const button = screen.getByRole('button')
    expect(button).toBeDisabled()
    expect(button).toHaveAttribute('aria-busy', 'true')
  })
  
  it('supports different sizes', () => {
    render(Button, { 
      size: 'lg',
      children: 'Large button'
    })
    
    expect(screen.getByRole('button')).toHaveClass('size-lg')
  })
})
#+END_SRC

* Phase 3: Documentation System [0/2]

** TODO Component Documentation Template
*** Standard Documentation Structure
#+BEGIN_SRC markdown
# Component Name

Brief description of the component's purpose and when to use it.

## Usage

```svelte
<Component prop="value">
  Content
</Component>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| size | 'sm' \| 'md' \| 'lg' | 'md' | Controls component size |

## Examples

### Basic Usage
[Interactive example]

### With Variants
[Interactive example]

## Accessibility

- Keyboard navigation: [details]
- Screen reader support: [details]
- ARIA attributes: [details]

## Design Guidelines

- When to use this component
- Visual hierarchy considerations
- Spacing and layout recommendations
#+END_SRC

** TODO Storybook Integration
*** Story Template
#+BEGIN_SRC typescript
// Button.stories.ts
import type { Meta, StoryObj } from '@storybook/svelte'
import Button from './Button.svelte'

const meta: Meta<Button> = {
  title: 'Components/Button',
  component: Button,
  parameters: {
    docs: {
      description: {
        component: 'Primary UI component for user actions'
      }
    }
  },
  argTypes: {
    size: {
      control: { type: 'select' },
      options: ['xs', 'sm', 'md', 'lg', 'xl']
    },
    variant: {
      control: { type: 'select' },
      options: ['primary', 'secondary', 'outline', 'ghost']
    }
  }
}

export default meta
type Story = StoryObj<typeof meta>

export const Primary: Story = {
  args: {
    children: 'Button'
  }
}

export const AllSizes: Story = {
  render: () => ({
    Component: ButtonSizes,
  })
}
#+END_SRC

* Implementation Timeline

** Week 1: Foundation Setup
- [X] Project analysis and planning
- [ ] Design token system implementation
- [ ] CSS architecture setup
- [ ] TypeScript type definitions

** Week 2: Component Enhancement
- [ ] Button component enhancement
- [ ] Input component enhancement  
- [ ] Card component enhancement
- [ ] Testing infrastructure setup

** Week 3: Standards & Documentation
- [ ] Component API standardization
- [ ] Storybook configuration
- [ ] Documentation templates
- [ ] Accessibility audit

** Week 4: Review & Refinement
- [ ] Code review and optimization
- [ ] Performance testing
- [ ] Documentation completion
- [ ] Phase 2 planning

* Success Criteria

** Design System Foundation
- [ ] All design tokens defined and implemented
- [ ] Consistent theming system with dark/light modes
- [ ] Accessible color contrast ratios (WCAG AA)
- [ ] Typography scale optimized for data interfaces

** Enhanced Components
- [ ] Button, Input, and Card components meet production standards
- [ ] All components support size variants and states
- [ ] Keyboard navigation and screen reader compatibility
- [ ] Loading and error states implemented

** Documentation & Testing
- [ ] Storybook stories for all enhanced components
- [ ] Comprehensive API documentation
- [ ] Unit tests with >80% coverage
- [ ] Accessibility testing passed

** Performance & Quality
- [ ] Bundle size impact <10% increase
- [ ] Lighthouse performance score >90
- [ ] Zero TypeScript errors
- [ ] ESLint rules compliance

* Next Steps

After Phase 1 completion:
1. Review implementation with stakeholders
2. Gather developer feedback on API design
3. Plan Phase 2: Data Management Components
4. Begin design work for HR-specific components
5. Prepare for component library publishing

* Resources & References

** Design System Examples
- [[https://designsystem.digital.gov/][U.S. Web Design System]]
- [[https://atlassian.design/][Atlassian Design System]]
- [[https://polaris.shopify.com/][Shopify Polaris]]

** Accessibility Guidelines
- [[https://www.w3.org/WAI/WCAG21/quickref/][WCAG 2.1 Quick Reference]]
- [[https://www.a11y-project.com/][The A11Y Project]]

** Component Libraries
- [[https://ui.shadcn.com/][shadcn/ui]] (React, similar approach)
- [[https://melt-ui.com/][Melt UI]] (Svelte headless components)
- [[https://www.skeleton.dev/][Skeleton]] (Svelte UI toolkit)